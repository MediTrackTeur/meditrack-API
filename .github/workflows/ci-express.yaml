name: CI Express

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  ci-express:
    uses: ./.github/workflows/ci-shared.yaml
    with:
      node-version: "20"
    secrets: inherit